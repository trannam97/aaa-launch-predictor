# Backend

FastAPI app. Endpoints for listing/tracking games, triggering predictions,
and serving data to the frontend. Does not itself run the scheduled jobs —
see `/jobs`.

See `PROJECT_SPEC.md` (repo root) — Backend section.
