# Data

Seed historical dataset (`historical_releases_seed.csv`) and DB migration
files (Alembic). The live `historical_releases` table grows from this seed
as tracked games resolve — see Prediction Lifecycle in `PROJECT_SPEC.md`.
