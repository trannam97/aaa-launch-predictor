# ML

Model training code and artifacts: the ordinal gradient-boosted classifier
(flop/underperform/success/breakout) and the company-tiering clustering
pipeline. Model files are gitignored — this folder holds training/inference
code, not committed weights.

See `PROJECT_SPEC.md` (repo root) — ML Model and Company Tiering sections.
